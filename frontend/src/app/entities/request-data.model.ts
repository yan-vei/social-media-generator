export class RequestDataUrl {
  constructor(
    public url: string
  ) {}
}

export class RequestDataText {
  constructor(
    public title: string,
    public text: string,
    public source: string
  ) {}
}
